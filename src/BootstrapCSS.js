import { Button } from "react-bootstrap";

function BootstrapCSS() {
  return (
    <>
      <Button onClick={() => alert("fff")}>Click</Button>
    </>
  );
}

export default BootstrapCSS;
